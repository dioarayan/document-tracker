    <%= bootstrap_form_with(model: @user, local: true, class: "signup-form") do |f| %>
        <%= f.text_field :username %>
        <%= f.email_field :email %>
        <%= f.password_field :password %>
        <%= f.submit "Register", class: "btn btn-primary mt-3" %>        
    <% end %>