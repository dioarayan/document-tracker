<div class="container">
    <div class="col-6">
        <h1> Sign up </h1>

            <%= bootstrap_form_with(model: @user, local: true) do |f| %>
                <%= f.text_field :username %>
                <%= f.email_field :email %>
                <%= f.primary %>
            <% end %>
            
    </div>
</div>